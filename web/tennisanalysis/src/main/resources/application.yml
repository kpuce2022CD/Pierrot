spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tennis_db
    username: pierrot
    password: 2019
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database: mysql
    database-platform: org.hibernate.dialect.MySQLInnoDBDialect
    properties:
      hibernate:
        "[show_sql]": true
        "[format_sql]": true
        "[use_sql_comments]": true
